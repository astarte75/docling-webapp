---
phase: 09-ui-improvements
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Background dot grid is visible in both light and dark mode"
    - "Entry animations stagger correctly: header first, then options, then upload zone"
    - "In success state, the layout is two-column with left sidebar and right ResultViewer"
    - "The compact header in success state shows logo + title (clickable) + ModeToggle"
    - "Clicking the logo/title in success state resets to idle single-column layout"
    - "The left sidebar 'Converti un altro file' compact upload zone accepts a new file and transitions to uploading state"
    - "All other states (idle, converting, error, batch) render in single-column max-w-3xl layout"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Conditional split layout verified visually"
  key_links:
    - from: "success sidebar UploadZone"
      to: "handleFilesSelected → uploading state"
      via: "onFilesSelected prop"
      pattern: "handleFilesSelected"
---

<objective>
Human verification of all Phase 9 UI improvements: background grid, entry animations, DoclingLogo, and the new split two-column layout in success state.

Purpose: Confirm the visual result matches the design decisions before the phase is marked complete.
Output: User approval or a list of issues to fix.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-improvements/09-CONTEXT.md
@.planning/phases/09-ui-improvements/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 UI improvements:
    1. Background dot grid (24px spacing) + emerald radial glow — implemented in index.css
    2. DoclingLogo SVG (document + fold + emerald # mark) — in header, centered above title
    3. Entry animations — header fades in first (0ms), options at 120ms, upload at 220ms
    4. Split two-column layout in success state — 340px left sidebar + flex-1 right ResultViewer panel
  </what-built>
  <how-to-verify>
    Start the dev server (if not already running):
      cd /Users/ricky/MyProjects/docling-webapp/frontend && npm run dev

    Open http://localhost:5173 in the browser.

    **Check 1 — Background and animations (idle state):**
    - Refresh the page. You should see the header fade-slide up first, then options panel, then upload zone (staggered ~120ms apart).
    - The background should show a subtle dot grid. In light mode: dark dots at low opacity. In dark mode: white dots at low opacity.
    - A soft emerald radial glow should be visible at the top-center of the page.

    **Check 2 — DoclingLogo:**
    - Above the "Docling Webapp" title, the SVG logo should appear: a document shape with a folded corner and an emerald # mark inside.
    - In dark mode the document body should adapt (uses currentColor).

    **Check 3 — Split layout (success state):**
    - Upload any supported document (PDF, DOCX, etc.).
    - After conversion completes, the layout should expand to two columns:
      - Left (340px): compact OptionsPanel + a small "Converti un altro file" compact upload zone
      - Right (flex-1): OCR badge row + ResultViewer with Preview/Raw tabs
    - The header at top should now be compact: logo + "Docling Webapp" on the left (clickable), ModeToggle on the right.
    - The page should NOT have the max-w-3xl centering constraint in success state — content should span full width.

    **Check 4 — Navigation from success state:**
    - Click the logo/title in the compact header. The app should reset to idle single-column layout.
    - Drop a file into the "Converti un altro file" compact zone in the left sidebar. The app should transition to uploading/converting state (single-column layout resumes).

    **Check 5 — Dark mode:**
    - Toggle dark mode via the ModeToggle button. Verify the split layout and background grid look correct in both modes.
  </how-to-verify>
  <resume-signal>
    Type "approved" if everything looks correct, or describe any visual issues to fix.
  </resume-signal>
</task>

</tasks>

<verification>
User has confirmed all five checks pass:
- Background grid visible, emerald glow present
- DoclingLogo renders correctly in light and dark mode
- Entry animations stagger as expected on page load
- Success state shows two-column split layout
- Navigation (reset to idle, convert another file) works correctly
</verification>

<success_criteria>
User approves the visual result. Phase 9 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-improvements/09-02-SUMMARY.md`
</output>

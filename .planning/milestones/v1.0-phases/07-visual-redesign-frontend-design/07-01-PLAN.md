---
phase: 07-visual-redesign-frontend-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/theme-provider.tsx
  - frontend/src/components/mode-toggle.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Elementi interattivi (pulsanti primari, focus ring, bordi) appaiono in verde smeraldo"
    - "Passando il cursore su un pulsante primario si vede il verde emerald"
    - "Il ThemeProvider esporta useTheme hook consumabile da altri componenti"
    - "Il ModeToggle mostra Sun in light mode e Moon in dark mode"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "CSS custom properties oklch emerald per primary, accent, ring in :root e .dark"
      contains: "oklch(59.6%"
    - path: "frontend/src/components/theme-provider.tsx"
      provides: "ThemeProvider context + useTheme hook"
      exports: ["ThemeProvider", "useTheme"]
    - path: "frontend/src/components/mode-toggle.tsx"
      provides: "Bottone Sun/Moon per alternare light/dark"
      exports: ["ModeToggle"]
  key_links:
    - from: "frontend/src/components/mode-toggle.tsx"
      to: "frontend/src/components/theme-provider.tsx"
      via: "useTheme hook import"
      pattern: "useTheme"
    - from: "frontend/src/index.css"
      to: "shadcn/ui components"
      via: "--primary, --accent, --ring CSS vars"
      pattern: "--primary: oklch"
---

<objective>
Implementare la palette emerald e il sistema di dark mode: aggiornare le CSS custom properties in index.css con valori oklch emerald, creare ThemeProvider e ModeToggle come componenti standalone pronti per il wiring in App.tsx.

Purpose: Stabilire l'identità cromatica emerald e l'infrastruttura dark mode prima di toccare i componenti specifici. Le CSS vars propagano automaticamente a tutti i componenti shadcn/ui senza modificarli.
Output: index.css aggiornato con palette emerald, theme-provider.tsx, mode-toggle.tsx
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-visual-redesign-frontend-design/07-CONTEXT.md
@.planning/phases/07-visual-redesign-frontend-design/07-RESEARCH.md

<interfaces>
<!-- Struttura CSS esistente in frontend/src/index.css -->
<!-- Il file usa @custom-variant dark (&:is(.dark *)) — NON modificare questa riga -->
<!-- Il blocco @theme inline mappa le CSS vars su Tailwind — NON modificare -->
<!-- Solo :root e .dark blocks devono essere aggiornati con i valori emerald -->

From frontend/src/index.css (struttura attuale da preservare):
```css
@import "tailwindcss";
@plugin "@tailwindcss/typography";
@import "tw-animate-css";
@import "shadcn/tailwind.css";

@custom-variant dark (&:is(.dark *));  /* NON toccare */

@theme inline { ... }  /* NON toccare — mappa vars su Tailwind */

:root { /* AGGIORNARE solo --primary, --accent, --ring */ }
.dark { /* AGGIORNARE solo --primary, --accent, --ring */ }

@layer base { ... }  /* NON toccare */
```

Vars shadcn/ui consumate dai componenti (propagazione automatica):
- --primary / --primary-foreground → Button default variant, active states
- --accent / --accent-foreground → hover backgrounds, focus states
- --ring → focus outline su tutti gli elementi interattivi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiornare palette emerald in index.css</name>
  <files>frontend/src/index.css</files>
  <action>
Aggiornare SOLO i valori --primary, --primary-foreground, --accent, --accent-foreground e --ring in :root e .dark. Lasciare invariati tutti gli altri valori (background, foreground, card, secondary, muted, destructive, border, input, chart-*, sidebar-*).

Valori ESATTI da usare (verificati da tailwindcolor.com):

In :root:
- --primary: oklch(59.6% .145 163.225);         /* emerald-600 */
- --primary-foreground: oklch(0.985 0 0);
- --accent: oklch(97.9% .021 166.113);           /* emerald-50 */
- --accent-foreground: oklch(37.8% .077 168.94); /* emerald-900 */
- --ring: oklch(69.6% .17 162.48);              /* emerald-500 */

In .dark:
- --primary: oklch(76.5% .177 163.223);          /* emerald-400 */
- --primary-foreground: oklch(26.2% .051 172.552); /* emerald-950 */
- --accent: oklch(26.2% .051 172.552);           /* emerald-950 */
- --accent-foreground: oklch(84.5% .143 164.978); /* emerald-300 */
- --ring: oklch(76.5% .177 163.223);             /* emerald-400 */

NON rimuovere le variabili sidebar. NON modificare @theme inline, @custom-variant dark, @layer base.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && grep -c "163.225\|163.223" src/index.css</automated>
  </verify>
  <done>grep restituisce almeno 2 (emerald in :root e .dark). La palette bianco/nero puro è sostituita da oklch emerald per primary, accent, ring.</done>
</task>

<task type="auto">
  <name>Task 2: Creare ThemeProvider e ModeToggle</name>
  <files>frontend/src/components/theme-provider.tsx, frontend/src/components/mode-toggle.tsx</files>
  <action>
Creare due nuovi file componenti:

**frontend/src/components/theme-provider.tsx** — Pattern ufficiale shadcn/ui per Vite. Gestisce: lettura da localStorage (lazy useState initializer), applicazione classe .dark su document.documentElement, ascolto di prefers-color-scheme per "system" default. storageKey fisso: "docling-theme".

```typescript
import { createContext, useContext, useEffect, useState } from "react"

type Theme = "dark" | "light" | "system"

type ThemeProviderProps = {
  children: React.ReactNode
  defaultTheme?: Theme
  storageKey?: string
}

type ThemeProviderState = {
  theme: Theme
  setTheme: (theme: Theme) => void
}

const initialState: ThemeProviderState = {
  theme: "system",
  setTheme: () => null,
}

const ThemeProviderContext = createContext<ThemeProviderState>(initialState)

export function ThemeProvider({
  children,
  defaultTheme = "system",
  storageKey = "docling-theme",
  ...props
}: ThemeProviderProps) {
  const [theme, setTheme] = useState<Theme>(
    () => (localStorage.getItem(storageKey) as Theme) || defaultTheme
  )

  useEffect(() => {
    const root = window.document.documentElement
    root.classList.remove("light", "dark")
    if (theme === "system") {
      const systemTheme = window.matchMedia("(prefers-color-scheme: dark)").matches
        ? "dark"
        : "light"
      root.classList.add(systemTheme)
      return
    }
    root.classList.add(theme)
  }, [theme])

  const value = {
    theme,
    setTheme: (theme: Theme) => {
      localStorage.setItem(storageKey, theme)
      setTheme(theme)
    },
  }

  return (
    <ThemeProviderContext.Provider {...props} value={value}>
      {children}
    </ThemeProviderContext.Provider>
  )
}

export const useTheme = () => {
  const context = useContext(ThemeProviderContext)
  if (context === undefined)
    throw new Error("useTheme must be used within a ThemeProvider")
  return context
}
```

**frontend/src/components/mode-toggle.tsx** — Toggle discreto Sun/Moon. Cicla tra light e dark (ignora "system" dopo la prima scelta manuale). Usa Button variant="ghost" size="icon" da shadcn. Le icone Sun e Moon usano classi Tailwind per transizione scale+rotate: Sun visibile in light (scale-100), Moon visibile in dark (scale-100). Entrambe `absolute` per sovrapposizione corretta (il Button ha `relative overflow-hidden` implicito in size="icon").

```typescript
import { Sun, Moon } from "lucide-react"
import { Button } from "@/components/ui/button"
import { useTheme } from "@/components/theme-provider"

export function ModeToggle() {
  const { theme, setTheme } = useTheme()

  const isDark =
    theme === "dark" ||
    (theme === "system" &&
      window.matchMedia("(prefers-color-scheme: dark)").matches)

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(isDark ? "light" : "dark")}
      aria-label="Toggle theme"
    >
      <Sun className="h-4 w-4 scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
      <Moon className="absolute h-4 w-4 scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
    </Button>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compila senza errori sui due nuovi file. theme-provider.tsx esporta ThemeProvider e useTheme. mode-toggle.tsx esporta ModeToggle e importa useTheme da theme-provider.</done>
</task>

</tasks>

<verification>
- `grep -c "163.225\|163.223" frontend/src/index.css` restituisce 2+
- `frontend/src/components/theme-provider.tsx` esiste e contiene export ThemeProvider e useTheme
- `frontend/src/components/mode-toggle.tsx` esiste e contiene export ModeToggle
- `npx tsc --noEmit` non riporta errori nei nuovi file
</verification>

<success_criteria>
Le CSS vars emerald sono in index.css. ThemeProvider e ModeToggle esistono e compilano. I componenti shadcn/ui esistenti riceveranno automaticamente il colore emerald quando l'app viene avviata — senza modifiche ai file ui/*.
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-frontend-design/07-01-SUMMARY.md`
</output>

---
phase: 07-visual-redesign-frontend-design
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Il redesign visivo emerald è verificato visivamente dall'utente"
    - "Il toggle dark/light funziona e la preferenza persiste dopo refresh"
    - "Il glow drag-over sull'UploadZone è visibile e non eccessivo"
    - "Il ResultViewer mostra titoli con gerarchia visiva chiara"
  artifacts: []
  key_links: []
---

<objective>
Verifica umana del redesign visivo completo: palette emerald, dark mode toggle, UploadZone glow, ResultViewer tipografia.

Purpose: Il redesign è puramente visivo — solo un umano può confermare che l'aspetto risultante soddisfa le aspettative estetiche definite in CONTEXT.md.
Output: Approvazione umana → Phase 7 completa.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-frontend-design/07-CONTEXT.md
@.planning/phases/07-visual-redesign-frontend-design/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avviare il dev server per verifica visiva</name>
  <files></files>
  <action>
Avviare il frontend in modalità development per permettere la verifica visiva:

```bash
cd /Users/ricky/MyProjects/docling-webapp/frontend && npm run dev
```

Il server sarà disponibile su http://localhost:5173 (o la porta configurata da Vite).

Questo task serve solo ad avviare il server — la verifica è nel checkpoint successivo.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build completa senza errori — il bundle di produzione è valido.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Redesign visivo completo della Phase 7:
- Palette emerald: pulsanti, focus ring, hover states in verde smeraldo
- Dark mode toggle Sun/Moon nell'header (destra del titolo "Docling Webapp")
- UploadZone: bordo verde solido in idle, glow emerald + sfondo tenue al drag-over
- ResultViewer: gerarchia titoli h1 > h2 > h3, line-height generoso, dark:prose-invert
  </what-built>
  <how-to-verify>
1. Aprire http://localhost:5173 nel browser
2. Verificare la palette emerald:
   - Il pulsante "Download .md" (se visibile) appare in verde smeraldo
   - L'UploadZone ha un bordo verde solido (non tratteggiato grigio)
3. Verificare il dark mode toggle:
   - Nell'header a destra del titolo "Docling Webapp" appare un'icona Sun (light) o Moon (dark)
   - Cliccarlo cambia l'interfaccia tra light e dark
   - Fare refresh — la scelta persiste (localStorage)
4. Verificare il sistema come default:
   - Se il sistema operativo è in dark mode, l'app parte in dark mode
   - Se in light mode, parte in light mode
5. Verificare l'UploadZone:
   - Drag di un file sull'area di upload: appare glow verde + sfondo tenue emerald
   - Il bordo diventa emerald solido più intenso durante il drag
6. Convertire un documento PDF e verificare il ResultViewer:
   - I titoli hanno dimensioni distinte (h1 grande, h2 medio, h3 più piccolo)
   - Il testo è ben spaziato e leggibile
   - In dark mode: testo bianco/chiaro su sfondo scuro (prose-invert attivo)
  </how-to-verify>
  <resume-signal>Digita "approvato" se tutto è corretto, oppure descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
Verifica manuale completata dall'utente con esito positivo.
</verification>

<success_criteria>
L'utente conferma:
- Verde smeraldo visibile su elementi interattivi chiave
- Toggle dark/light funzionante e persistente
- Glow drag-over sull'UploadZone elegante (non eccessivo)
- Tipografia ResultViewer leggibile con gerarchia visiva chiara in entrambi i temi
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-frontend-design/07-03-SUMMARY.md`
</output>

---
phase: 06-cancel-and-ux-fixes
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - UX-CANCEL-SINGLE
  - UX-CANCEL-BATCH
  - UX-HEADER-NAV
  - UX-RESET-SUCCESS

must_haves:
  truths:
    - "Cancel button in ConversionProgress cancels the ongoing conversion and restores the full hero UploadZone"
    - "X button in a converting BatchFileRow cancels that file and shows 'Annullato' badge; other files continue"
    - "Clicking the header 'Docling Webapp' from any app phase returns to idle"
    - "'Nuova conversione' button in success state returns to idle with full hero UploadZone"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "All UX controls wired"
      contains: "Nuova conversione"
  key_links:
    - from: "Human verification"
      to: "All four UX flows confirmed working"
      via: "Manual testing in browser"
      pattern: "n/a"
---

<objective>
Human verification that all four Phase 6 UX controls work correctly in the browser.

Purpose: Visual and interactive validation cannot be automated — the user confirms cancel
behaviors, state transitions, and UI appearance before the phase is marked complete.

Output: Phase 6 approved or issues identified for fixing.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cancel-and-ux-fixes/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: All four UX controls work in the browser</name>
  <files>none</files>
  <action>
    HUMAN VERIFICATION REQUIRED — no automated action for this task.

    Start the dev server if not already running:
    cd /Users/ricky/MyProjects/docling-webapp/frontend && npm run dev

    Then open http://localhost:5173 and run the four test scenarios below.
  </action>
  <verify>
    Manual browser testing — see how-to-verify section below.

    Test 1 — Cancel single-file conversion:
    1. Upload a document (PDF or DOCX)
    2. While the spinner is visible, click "Annulla"
    3. Expected: spinner disappears, full hero UploadZone is restored (not compact strip), app is back to idle

    Test 2 — Cancel batch item:
    1. Upload 3+ files at once (drag multiple files onto the zone)
    2. While at least one file shows "Conversione" status with the spinner, click the X button next to it
    3. Expected: that file's badge changes to "Annullato" (muted gray, not red), other files continue converting normally

    Test 3 — Header navigation:
    a. During single-file conversion (spinner visible): click "Docling Webapp" in the header
       Expected: app returns to idle, full hero UploadZone restored
    b. During batch mode (files in list): click "Docling Webapp"
       Expected: batch list clears, app returns to idle with full hero UploadZone

    Test 4 — "Nuova conversione" button:
    1. Upload and convert a single file to completion
    2. When the result is shown (with the compact strip at top), click "Nuova conversione"
    3. Expected: result disappears, full hero UploadZone restored (not the compact strip)

    Visual checks:
    - "Annullato" badge should be muted gray (same as "In attesa"), not red
    - "Docling Webapp" header should show pointer cursor on hover with slight opacity reduction
    - Cancel button in ConversionProgress should be a subtle ghost button below the spinner
    - "Nuova conversione" is an outline button aligned to the right of the "Conversione completata" label
  </verify>
  <done>
    User types "approved" confirming all four test scenarios pass.
    If issues are found, describe them so they can be fixed before marking complete.
  </done>
  <what-built>
    Plan 06-01 implemented all four UX controls:
    1. Cancel button in ConversionProgress (single-file flow)
    2. Per-item X cancel button in BatchFileRow (batch flow)
    3. Clickable header "Docling Webapp" for global reset
    4. "Nuova conversione" button in success state
  </what-built>
  <resume-signal>
    Type "approved" if all four tests pass, or describe any issues found (e.g., "Test 2 fails — X button not visible").
  </resume-signal>
</task>

</tasks>

<verification>
Human approval received for all four UX control flows.
</verification>

<success_criteria>
- User confirms all four cancel/reset mechanisms work as described in CONTEXT.md
- No visual regressions in existing functionality (upload, convert, download still work)
- Phase 6 approved for completion
</success_criteria>

<output>
After approval, create `.planning/phases/06-cancel-and-ux-fixes/06-02-SUMMARY.md` using the
summary template at `@/Users/ricky/.claude/get-shit-done/templates/summary.md`.
</output>

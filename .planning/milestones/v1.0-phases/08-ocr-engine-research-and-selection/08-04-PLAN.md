---
phase: 08-ocr-engine-research-and-selection
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User sees the OCR engine selector in the advanced options panel"
    - "Changing the engine and converting a document works end-to-end"
    - "Selecting an engine that is not installed surfaces a clear error message"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete OCR engine selection feature: UI selector visible and functional, end-to-end conversion working with engine selection, and error messaging for unavailable engines confirmed.

Purpose: Confirm that plans 08-01 (backend) and 08-02 (frontend) integrate correctly and the user experience matches decisions.
Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ocr-engine-research-and-selection/08-CONTEXT.md
@.planning/phases/08-ocr-engine-research-and-selection/08-01-SUMMARY.md
@.planning/phases/08-ocr-engine-research-and-selection/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-check before human verification</name>
  <files></files>
  <action>
Run automated checks to confirm implementation is complete before asking for human review:

1. TypeScript compiles without errors:
```bash
cd /Users/ricky/MyProjects/docling-webapp && npx tsc --noEmit -p frontend/tsconfig.app.json
```

2. Verify ocrEngine is present in all expected files:
```bash
grep -r "ocrEngine\|ocr_engine" /Users/ricky/MyProjects/docling-webapp/frontend/src/ /Users/ricky/MyProjects/docling-webapp/backend/
```

3. Verify backend engine factory is correct:
```bash
python -c "
import sys; sys.path.insert(0, '/Users/ricky/MyProjects/docling-webapp/backend')
from adapter import _OCR_ENGINE_CLASSES, ConversionOptions, build_pipeline_options
assert set(_OCR_ENGINE_CLASSES.keys()) == {'auto','easyocr','rapidocr','tesseract'}
print('Backend engine factory: OK')
"
```

If any check fails, fix the issue before proceeding to human review.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp && npx tsc --noEmit -p frontend/tsconfig.app.json && echo "TS OK"</automated>
  </verify>
  <done>All automated checks pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete OCR engine selection feature:
- Backend: 4-engine factory in adapter.py (Auto/EasyOCR/RapidOCR/Tesseract), ocr_engine Form field in /convert, engine-missing error surfacing
- Frontend: OcrEngine type in job.ts, OCR engine Select in OptionsPanel advanced section, ocrEngine wired to FormData
- Dockerfile + requirements.txt: rapidocr-onnxruntime and tesserocr added; Tesseract system packages installed
  </what-built>
  <how-to-verify>
1. Start the app: `docker compose up` (or dev server if not using Docker)

2. Open http://localhost:3000 in the browser

3. Click "Opzioni avanzate" to expand the advanced panel
   Expected: A new "Motore OCR" selector appears above the language select
   Expected: The selector shows 4 options — "Auto (best available)", "EasyOCR", "RapidOCR (fast)", "Tesseract"
   Expected: Default selected value is "Auto (best available)"
   Expected: Language select shows "(EasyOCR)" hint in its label

4. Select "EasyOCR", upload a PDF, and convert
   Expected: Conversion completes successfully

5. Select "Auto (best available)", upload a PDF, and convert
   Expected: Conversion completes successfully

6. (Optional — only if Tesseract/RapidOCR are installed in Docker image) Select "Tesseract" or "RapidOCR" and convert
   Expected: Conversion completes, OR a clear error message appears if the package is not installed

7. Confirm the engine selector persists when switching between files
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval confirms:
- Engine selector visible in advanced panel with correct labels and default
- End-to-end conversion works with at least Auto and EasyOCR engines
- Language hint "(EasyOCR)" visible
</verification>

<success_criteria>
Human confirms: OCR engine selector is visible, functional, and end-to-end conversion works correctly with the selected engine.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ocr-engine-research-and-selection/08-04-SUMMARY.md`

Then update STATE.md:
- Current Position: Phase 8 COMPLETE
- Progress: Phase 8 of 9 complete
</output>

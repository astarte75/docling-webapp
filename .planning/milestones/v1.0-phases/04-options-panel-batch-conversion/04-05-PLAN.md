---
phase: 04-options-panel-batch-conversion
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-03"
  - "04-04"
files_modified:
  - frontend/src/components/UploadZone.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - BTCH-01
  - BTCH-02
  - BTCH-03
  - BTCH-04

must_haves:
  truths:
    - "L'utente vede OptionsPanel sopra l'upload zone in tutti gli stati non-batch"
    - "1 file droppato → flusso single-file esistente (invariato)"
    - "2+ file droppati → AppPhase 'batch-active', OptionsPanel resta visibile (compatto), BatchList appare"
    - "Le opzioni sono snapshotate al drop time — cambiare opzioni dopo il drop non influenza file già in coda"
    - "Il pulsante '+  Aggiungi altri file' è visibile nello stato batch-active, permette di aggiungere file con le opzioni correnti"
    - "UploadZone accetta multiple files via prop"
  artifacts:
    - path: "frontend/src/components/UploadZone.tsx"
      provides: "UploadZone esteso con supporto multiple files via onFilesSelected"
      exports: ["UploadZone"]
    - path: "frontend/src/App.tsx"
      provides: "State machine App.tsx aggiornato: OptionsPanel, batch-active, batch-complete, single-file invariato"
      exports: ["App (default)"]
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/OptionsPanel.tsx"
      via: "currentOptions state → OptionsPanel value + onChange"
      pattern: "currentOptions.*ConversionOptions"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/useBatchUpload.ts"
      via: "useBatchUpload().addFiles(files, currentOptions)"
      pattern: "addFiles.*currentOptions"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/BatchList.tsx"
      via: "state.phase === 'batch-active' → <BatchList files={batchHook.files} />"
      pattern: "batch-active"
---

<objective>
Integra tutto in App.tsx: OptionsPanel, flusso batch, UploadZone multi-file, e punto di verifica finale.

Purpose: Ultimo piano della Phase 4 — collega backend aggiornato, tipi, OptionsPanel e BatchList nel state machine di App.tsx. Aggiunge il checkpoint di verifica visiva.

Output: UploadZone esteso con multiple, App.tsx aggiornato con OptionsPanel + batch state, checkpoint umano di verifica.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/04-options-panel-batch-conversion/04-CONTEXT.md
@.planning/phases/04-options-panel-batch-conversion/04-RESEARCH.md

@.planning/phases/04-options-panel-batch-conversion/04-03-SUMMARY.md
@.planning/phases/04-options-panel-batch-conversion/04-04-SUMMARY.md

<interfaces>
<!-- Tutti i pezzi da collegare -->

From frontend/src/components/OptionsPanel.tsx (after 04-03):
```typescript
export function OptionsPanel({
  value: ConversionOptions,
  onChange: (opts: ConversionOptions) => void
}): JSX.Element
```

From frontend/src/hooks/useBatchUpload.ts (after 04-04):
```typescript
export function useBatchUpload(): {
  files: BatchFile[];
  addFiles: (newFiles: File[], opts: ConversionOptions) => Promise<void>;
  retryFile: (itemId: string) => Promise<void>;
  clearFiles: () => void;
}
```

From frontend/src/components/BatchList.tsx (after 04-04):
```typescript
export function BatchList({
  files: BatchFile[],
  onRetry: (id: string) => void
}): JSX.Element
```

From frontend/src/types/job.ts (after 04-02):
```typescript
export type AppPhase =
  | { phase: 'idle' }
  | { phase: 'uploading'; file: File }
  | { phase: 'converting'; jobId: string; file: File }
  | { phase: 'success'; markdown: string; filename: string }
  | { phase: 'error'; message: string; file: File }
  | { phase: 'batch-active'; files: BatchFile[] }
  | { phase: 'batch-complete'; files: BatchFile[] };

export const DEFAULT_CONVERSION_OPTIONS: ConversionOptions;
```

From frontend/src/components/UploadZone.tsx (current — da estendere):
```typescript
interface UploadZoneProps {
  onFileSelected: (file: File) => void;  // da rinominare/affiancare
  disabled?: boolean;
}
// maxFiles: 1 hardcoded — da rendere configurabile
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estendi UploadZone per supportare multiple files</name>
  <files>frontend/src/components/UploadZone.tsx</files>
  <action>
Aggiorna frontend/src/components/UploadZone.tsx per supportare sia single che multi-file:

**Nuova props interface:**
```typescript
interface UploadZoneProps {
  onFilesSelected: (files: File[]) => void;  // nuova prop (rimpiazza onFileSelected)
  disabled?: boolean;
  compact?: boolean;  // true = versione strip compatta (usata in batch-active e success)
}
```

**Aggiornamenti al corpo:**
1. Rimuovi `maxFiles: 1` da useDropzone → usa `multiple: true`
2. `onDropAccepted: (files) => { setError(null); onFilesSelected(files); }` (files è array)
3. Rimuovi il case `ErrorCode.TooManyFiles` dal getRejectionMessage (non più rilevante)
4. Aggiorna il testo: "Drag and drop files, or click to browse" (non più "a PDF")
5. Aggiorna il sotto-testo: "PDF only · max 50MB per file"
6. Se `compact={true}`: mostra solo l'input minimo (stile strip), senza icona cloud grande
   - In compact: `<div className="w-full cursor-pointer rounded-md border px-3 py-1.5 text-sm text-muted-foreground hover:border-primary/60 hover:text-foreground">`
   - Testo compatto: "Aggiungi altri file..."

**NOTA RETROCOMPATIBILITÀ:** App.tsx usa attualmente `onFileSelected` (singolo). Dopo questa modifica, App.tsx dovrà essere aggiornato nel Task 2 di questo piano. Il vecchio `onFileSelected` viene rimosso — è l'unico consumer.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>
    - UploadZone accetta multiple files (multiple: true in useDropzone)
    - Props: onFilesSelected (non più onFileSelected), disabled, compact
    - Compact mode: versione strip "Aggiungi altri file..."
    - Testi aggiornati per multi-file
  </done>
</task>

<task type="auto">
  <name>Task 2: App.tsx — state machine con OptionsPanel + batch flow</name>
  <files>frontend/src/App.tsx</files>
  <action>
Riscrivi frontend/src/App.tsx mantenendo tutto il flusso single-file e aggiungendo batch e OptionsPanel.

**Schema del nuovo App.tsx:**

```typescript
import { useState, useCallback } from 'react';
import { useUpload } from '@/hooks/useUpload';
import { useJobStream } from '@/hooks/useJobStream';
import { useBatchUpload } from '@/hooks/useBatchUpload';
import { UploadZone } from '@/components/UploadZone';
import { OptionsPanel } from '@/components/OptionsPanel';
import { ConversionProgress } from '@/components/ConversionProgress';
import { ResultViewer } from '@/components/ResultViewer';
import { BatchList } from '@/components/BatchList';
import { Button } from '@/components/ui/button';
import type { AppPhase, ConversionOptions } from '@/types/job';
import { DEFAULT_CONVERSION_OPTIONS } from '@/types/job';

export default function App() {
  const [state, setState] = useState<AppPhase>({ phase: 'idle' });
  // Options state — sticky for session, reset on refresh (no localStorage)
  const [currentOptions, setCurrentOptions] = useState<ConversionOptions>(DEFAULT_CONVERSION_OPTIONS);
  const upload = useUpload();
  const batchHook = useBatchUpload();

  // Handler per files dropped — decide single vs batch flow
  const handleFilesSelected = useCallback(async (files: File[]) => {
    if (files.length === 1) {
      // Single-file flow — come prima
      const file = files[0];
      setState({ phase: 'uploading', file });
      // Per il single-file, usa useUpload (TanStack Mutation) — mantiene l'upload progress se presente
      // NOTA: useUpload non invia options — per ora il single-file usa le opzioni default del backend
      // Per inviare options anche nel single-file: creare un formData manuale qui (miglioramento v2)
      try {
        const formData = new FormData();
        formData.append('file', file);
        formData.append('ocr_mode', currentOptions.ocrMode);
        formData.append('table_detection', String(currentOptions.tableDetection));
        if (currentOptions.pageFrom !== null) formData.append('page_from', String(currentOptions.pageFrom));
        if (currentOptions.pageTo !== null) formData.append('page_to', String(currentOptions.pageTo));
        if (currentOptions.ocrLanguages.length > 0) formData.append('ocr_languages', currentOptions.ocrLanguages.join(','));

        const res = await fetch('/api/convert', { method: 'POST', body: formData });
        if (!res.ok) {
          const body = await res.json().catch(() => ({ error: 'Upload failed' }));
          throw new Error(body.error || 'Upload failed');
        }
        const { job_id } = await res.json();
        setState({ phase: 'converting', jobId: job_id, file });
      } catch (err) {
        const message = err instanceof Error ? err.message : 'Upload failed';
        setState({ phase: 'error', message, file });
      }
    } else {
      // Batch flow — 2+ files
      setState({ phase: 'batch-active', files: [] });
      await batchHook.addFiles(files, currentOptions);
    }
  }, [currentOptions, batchHook]);

  // Single-file SSE
  const jobId = state.phase === 'converting' ? state.jobId : null;
  useJobStream(jobId, {
    onCompleted: useCallback((markdown: string) => {
      setState((prev) => ({
        phase: 'success',
        markdown,
        filename: prev.phase === 'converting' ? prev.file.name : 'document',
      }));
    }, []),
    onFailed: useCallback((message: string) => {
      setState((prev) => ({
        phase: 'error',
        message,
        file: prev.phase === 'converting' ? prev.file : new File([], 'unknown'),
      }));
    }, []),
  });

  const isConverting = state.phase === 'uploading' || state.phase === 'converting';
  const isBatch = state.phase === 'batch-active' || state.phase === 'batch-complete';

  return (
    <div className="min-h-screen bg-background text-foreground">
      <div className="mx-auto max-w-3xl px-4 py-8">

        {/* Header */}
        <header className="mb-8 text-center">
          <h1 className="text-2xl font-semibold tracking-tight">Docling Webapp</h1>
          <p className="mt-1 text-sm text-muted-foreground">
            Convert documents to Markdown
          </p>
        </header>

        {/* Options Panel — visible in idle and batch-active (above upload zone) */}
        {(state.phase === 'idle' || isBatch) && (
          <div className="mb-4">
            <OptionsPanel value={currentOptions} onChange={setCurrentOptions} />
          </div>
        )}

        {/* Upload zone area */}
        <div className={isBatch || state.phase === 'success' ? 'mb-4' : 'mb-8'}>
          {isBatch ? (
            /* Batch compact strip */
            <div className="flex items-center gap-3 rounded-lg border bg-muted/30 px-4 py-3">
              <span className="text-sm text-muted-foreground">Aggiungi altri file:</span>
              <div className="flex-1">
                <UploadZone
                  onFilesSelected={(files) => batchHook.addFiles(files, currentOptions)}
                  disabled={false}
                  compact
                />
              </div>
            </div>
          ) : state.phase === 'success' ? (
            /* Single-file success compact strip */
            <div className="flex items-center gap-3 rounded-lg border bg-muted/30 px-4 py-3">
              <span className="text-sm text-muted-foreground">Converti un altro file:</span>
              <div className="flex-1">
                <UploadZone
                  onFilesSelected={handleFilesSelected}
                  disabled={isConverting}
                  compact
                />
              </div>
            </div>
          ) : (
            /* Hero upload zone (idle, converting, error) */
            <UploadZone
              onFilesSelected={handleFilesSelected}
              disabled={isConverting}
            />
          )}
        </div>

        {/* Converting state — single file */}
        {isConverting && <ConversionProgress />}

        {/* Error state — single file */}
        {state.phase === 'error' && (
          <div className="flex flex-col items-center gap-4 rounded-lg border border-destructive/30 bg-destructive/5 px-6 py-8 text-center">
            <p className="text-sm font-medium text-destructive" role="alert">
              {state.message}
            </p>
            <Button
              variant="outline"
              onClick={() => handleFilesSelected([state.file])}
            >
              Riprova
            </Button>
          </div>
        )}

        {/* Success state — single file */}
        {state.phase === 'success' && (
          <div className="rounded-lg border">
            <div className="border-b px-4 py-3">
              <p className="text-sm font-medium text-muted-foreground">
                Conversione completata
              </p>
            </div>
            <ResultViewer
              markdown={state.markdown}
              filename={state.filename}
            />
          </div>
        )}

        {/* Batch state */}
        {isBatch && batchHook.files.length > 0 && (
          <BatchList
            files={batchHook.files}
            onRetry={batchHook.retryFile}
          />
        )}

      </div>
    </div>
  );
}
```

NOTA: Il flusso single-file viene leggermente modificato: invece di passare attraverso `useUpload` (TanStack Mutation), usa un fetch diretto per poter includere i form fields delle opzioni. `useUpload` resta invariato ma non viene più usato in App.tsx — non rimuovere il file, potrebbe servire come riferimento.

NOTA: OptionsPanel è visibile solo in `idle` e `batch-active`. Non appare durante il converting o success del flusso single-file per non distrarre durante la conversione.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - App.tsx compila e Vite build passa
    - OptionsPanel visibile in idle e batch-active
    - 1 file → flusso single-file con opzioni (fetch diretto con FormData)
    - 2+ file → batch-active, BatchList visibile
    - Compact UploadZone strip in batch-active e success
    - Retry single-file funziona
    - currentOptions snapshotato al drop time via spread { ...currentOptions }
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica visiva Phase 4 — options panel e batch conversion</name>
  <what-built>
    Phase 4 completa: OptionsPanel con OCR ToggleGroup + advanced panel collassabile, batch multi-file con status per-file, ZIP download client-side, backend con PipelineOptions e semaphore concurrency.
  </what-built>
  <how-to-verify>
    1. Avvia l'app: `docker compose up` (dalla root del progetto)
    2. Apri il browser su http://localhost:3000

    **Test OCR e options panel (CONF-01 → CONF-05):**
    3. Verifica che sopra l'upload zone siano visibili i 3 pulsanti [Auto] [On] [Off] per OCR mode
    4. Clicca "Opzioni avanzate" — verifica che il pannello si espanda verso il basso con: toggle tabelle, campi page range (da/a), selector lingua OCR
    5. Cambia qualcosa (es. OCR su "On"), poi carica un PDF — verifica che la conversione avvenga

    **Test single-file con options (retrocompatibilità):**
    6. Con un PDF singolo, setta OCR mode su "Off", droppalo — verifica che funzioni come prima (progress → result)

    **Test batch (BTCH-01 → BTCH-04):**
    7. Seleziona o droppa 2+ PDF contemporaneamente
    8. Verifica che appaia la batch list con filename + status badge per ogni file
    9. Verifica che durante la conversione le righe mostrino lo spinner "Conversione"
    10. Verifica che a fine conversione appaia il pulsante "Scarica ZIP (N file)"
    11. Clicca "Scarica ZIP" — verifica che si scarichi un file .zip con i markdown dentro

    **Test retry (batch error):**
    12. Se un file fallisce, verifica che appaia il pulsante "Riprova" e che funzioni

    **Test "Aggiungi altri file" (mid-batch):**
    13. Con batch in corso, usa il compact strip in cima per aggiungere altri file
    14. Verifica che i nuovi file appaiano in coda nella batch list
  </how-to-verify>
  <action>Human verification step — no automated action. User tests the UI manually following the steps in how-to-verify.</action>
  <resume-signal>Digita "approvato" o descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
```bash
# Build finale
cd /Users/ricky/MyProjects/docling-webapp/frontend
npx tsc --noEmit && npm run build

# Avvio stack completo
cd /Users/ricky/MyProjects/docling-webapp
docker compose up --build
```
</verification>

<success_criteria>
- UploadZone accetta multiple files, prop onFilesSelected (non più onFileSelected)
- App.tsx compila e il build Vite passa
- OptionsPanel visibile sopra l'upload zone in idle e batch-active
- 1 file → flusso single-file con options inviate al backend
- 2+ file → batch-active, BatchList, ZIP download
- Checkpoint umano: utente verifica l'interfaccia completa e digita "approvato"
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/04-options-panel-batch-conversion/04-05-SUMMARY.md`
</output>

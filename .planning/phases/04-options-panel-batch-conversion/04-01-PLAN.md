---
phase: 04-options-panel-batch-conversion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/adapter.py
  - backend/job_store.py
  - backend/main.py
autonomous: true
requirements:
  - CONF-01
  - CONF-03
  - CONF-04
  - CONF-05
  - BTCH-03

must_haves:
  truths:
    - "POST /convert accetta form fields ocr_mode, table_detection, page_from, page_to, ocr_languages accanto al file"
    - "Il backend converte con opzioni personalizzate per-job (OCR mode, table detection, page range, OCR language)"
    - "Il worker esegue al massimo MAX_CONCURRENT_JOBS=2 conversioni in parallelo (configurabile via env var)"
    - "La variabile d'ambiente MAX_CONCURRENT_JOBS è documentata in config.py con default 2"
  artifacts:
    - path: "backend/config.py"
      provides: "MAX_CONCURRENT_JOBS env var"
      contains: "MAX_CONCURRENT_JOBS"
    - path: "backend/adapter.py"
      provides: "ConversionOptions dataclass, build_pipeline_options, DoclingAdapter.convert_file con options param"
      exports: ["ConversionOptions", "DoclingAdapter"]
    - path: "backend/job_store.py"
      provides: "Job con options field, semaphore-based conversion_worker"
      contains: "asyncio.Semaphore"
    - path: "backend/main.py"
      provides: "POST /convert che legge form fields e costruisce ConversionOptions"
      contains: "Form(default="
  key_links:
    - from: "backend/main.py POST /convert"
      to: "backend/adapter.py ConversionOptions"
      via: "Form params → ConversionOptions instantiation → job.options"
      pattern: "ConversionOptions\\("
    - from: "backend/job_store.py conversion_worker"
      to: "backend/adapter.py convert_file"
      via: "await state.converter.convert_file(job.tmp_path, job.options)"
      pattern: "convert_file.*job\\.options"
---

<objective>
Aggiorna il backend per supportare opzioni di conversione per-job e concorrenza limitata.

Purpose: Il frontend della Phase 4 invierà OCR mode, table detection, page range e OCR language come form fields nel POST /convert. Il worker deve supportare queste opzioni e processare massimo 2 job in parallelo.

Output: Backend aggiornato con ConversionOptions dataclass, DoclingAdapter che accetta opzioni, conversion_worker con asyncio.Semaphore, endpoint /convert che legge i form fields.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-options-panel-batch-conversion/04-CONTEXT.md
@.planning/phases/04-options-panel-batch-conversion/04-RESEARCH.md

<interfaces>
<!-- Stato attuale del backend — da estendere in questo piano -->

From backend/config.py (current):
```python
MAX_UPLOAD_SIZE_BYTES = int(os.getenv("MAX_UPLOAD_SIZE_MB", "50")) * 1024 * 1024
ALLOWED_EXTENSIONS = {".pdf"}
```

From backend/adapter.py (current):
```python
class DoclingAdapter:
    def __init__(self) -> None:
        self._converter = DocumentConverter()
        self._lock = asyncio.Lock()

    async def convert_file(self, file_path: str) -> str:
        # Usa singleton DocumentConverter senza opzioni
```

From backend/job_store.py (current):
```python
@dataclass
class Job:
    job_id: str
    tmp_path: str
    events: asyncio.Queue = field(default_factory=asyncio.Queue)
    status: str = "queued"

async def conversion_worker(state) -> None:
    # FIFO singolo worker, un job alla volta
    while True:
        job_id = await state.dispatch_queue.get()
        # ...
        markdown = await state.converter.convert_file(job.tmp_path)
```

From backend/main.py (current):
```python
@app.post("/convert")
async def convert(request: Request, file: UploadFile) -> JSONResponse:
    # Solo file, nessun form field per le opzioni
    job = Job(job_id=job_id, tmp_path=tmp_path)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConversionOptions dataclass + DoclingAdapter aggiornato</name>
  <files>backend/adapter.py</files>
  <behavior>
    - ConversionOptions(ocr_mode="auto") → PdfPipelineOptions con do_ocr=True, force_full_page_ocr=False
    - ConversionOptions(ocr_mode="on") → do_ocr=True, force_full_page_ocr=True
    - ConversionOptions(ocr_mode="off") → do_ocr=False
    - ConversionOptions(table_detection=False) → do_table_structure=False
    - ConversionOptions(page_from=2, page_to=5) → convert() chiamato con page_range=(2, 5)
    - ConversionOptions(page_from=3, page_to=None) → page_range=(3, 99999)
    - ConversionOptions(ocr_languages=["it", "en"]) → EasyOcrOptions.lang=["it", "en"]
    - convert_file(path, ConversionOptions()) funziona con tutti i default
  </behavior>
  <action>
Aggiungi a backend/adapter.py:

1. Importa: `from dataclasses import dataclass, field`, `from typing import Optional`, `from docling.document_converter import PdfFormatOption`, `from docling.datamodel.pipeline_options import PdfPipelineOptions, EasyOcrOptions`

2. Definisci dataclass ConversionOptions sopra DoclingAdapter:
```python
@dataclass
class ConversionOptions:
    ocr_mode: str = "auto"           # "auto" | "on" | "off"
    table_detection: bool = True
    page_from: Optional[int] = None  # 1-based inclusive
    page_to: Optional[int] = None    # 1-based inclusive, None = last page
    ocr_languages: list[str] = field(default_factory=list)  # EasyOCR lang codes
```

3. Aggiungi funzione `build_pipeline_options(opts: ConversionOptions) -> PdfPipelineOptions`:
   - Se ocr_mode == "off": `PdfPipelineOptions(do_ocr=False, do_table_structure=opts.table_detection)`
   - Se ocr_mode == "auto": `PdfPipelineOptions(do_ocr=True, do_table_structure=opts.table_detection)` con EasyOcrOptions (force_full_page_ocr=False), aggiungi lang se non vuoto
   - Se ocr_mode == "on": come auto ma force_full_page_ocr=True

4. Aggiorna `convert_file` signature: `async def convert_file(self, file_path: str, opts: Optional[ConversionOptions] = None) -> str`
   - Se opts è None, usa ConversionOptions() (default)
   - Crea DocumentConverter PER REQUEST con le opzioni del job usando format_options:
     ```python
     pipeline_options = build_pipeline_options(opts)
     converter = DocumentConverter(
         format_options={"application/pdf": PdfFormatOption(pipeline_options=pipeline_options)}
     )
     ```
   - Rimuovi self._converter dal __init__ (non serve più il singleton con opzioni fisse)
   - Mantieni self._lock — copre creazione+conversione nell'intero blocco `async with self._lock:`
   - Per page_range: se page_from non è None, passa `page_range=(opts.page_from, opts.page_to or 99999)` a `converter.convert()`
   - Mantieni check `result.status.value != "success"` e export_to_markdown()

NOTA: Il singleton DocumentConverter viene rimosso dal __init__ perché Docling bundla le PipelineOptions al momento della costruzione. Il lock garantisce che non ci siano race condition anche con la creazione per-request.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/backend && python -c "from adapter import ConversionOptions, DoclingAdapter; opts = ConversionOptions(ocr_mode='off'); print('ConversionOptions OK:', opts)"</automated>
  </verify>
  <done>
    - ConversionOptions importabile con tutti i campi default
    - build_pipeline_options produce PdfPipelineOptions corretta per tutti e tre gli ocr_mode
    - DoclingAdapter.convert_file accetta opts: Optional[ConversionOptions]
    - Il lock copre creazione+conversione
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job con options field + conversion_worker con asyncio.Semaphore</name>
  <files>backend/job_store.py, backend/config.py</files>
  <behavior>
    - Job dataclass ha campo options: ConversionOptions
    - conversion_worker con semaphore(2) non processa più di 2 job contemporaneamente
    - task_done() chiamato nel finally — invariante mantenuto
    - Worker non si blocca: legge job dalla queue e spawna asyncio.create_task() immediatamente
    - MAX_CONCURRENT_JOBS letto da env var con default "2"
  </behavior>
  <action>
**backend/config.py** — aggiungi:
```python
MAX_CONCURRENT_JOBS = int(os.getenv("MAX_CONCURRENT_JOBS", "2"))
```

**backend/job_store.py** — riscrivere completamente:

1. Importa ConversionOptions da adapter: `from adapter import ConversionOptions`
2. Importa MAX_CONCURRENT_JOBS da config: `from config import MAX_CONCURRENT_JOBS`
3. Aggiorna Job dataclass — aggiungi campo options:
   ```python
   @dataclass
   class Job:
       job_id: str
       tmp_path: str
       options: ConversionOptions = field(default_factory=ConversionOptions)
       events: asyncio.Queue = field(default_factory=asyncio.Queue)
       status: str = "queued"
   ```

4. Riscrivi conversion_worker con semaphore pattern:
   ```python
   async def conversion_worker(state) -> None:
       semaphore = asyncio.Semaphore(MAX_CONCURRENT_JOBS)

       async def process_job(job_id: str) -> None:
           job = state.jobs[job_id]
           async with semaphore:
               try:
                   job.status = "converting"
                   await job.events.put({"type": "started", "message": "Conversion started"})
                   markdown = await state.converter.convert_file(job.tmp_path, job.options)
                   job.status = "completed"
                   await job.events.put({"type": "completed", "markdown": markdown})
               except RuntimeError as exc:
                   job.status = "failed"
                   await job.events.put({"type": "failed", "message": str(exc)})
               except Exception:
                   logger.exception("Unexpected error processing job %s", job_id)
                   job.status = "failed"
                   await job.events.put({"type": "failed", "message": "Internal server error"})
               finally:
                   if os.path.exists(job.tmp_path):
                       os.remove(job.tmp_path)
                   state.dispatch_queue.task_done()

       while True:
           job_id = await state.dispatch_queue.get()
           asyncio.create_task(process_job(job_id))
   ```

   NOTA CRITICA: `asyncio.create_task(process_job(job_id))` è fire-and-forget nel loop; il semaphore limita quanti sono in `convert_file` contemporaneamente. Il `task_done()` è nel finally di process_job — non nel loop principale.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/backend && python -c "from job_store import Job, conversion_worker; from adapter import ConversionOptions; j = Job(job_id='test', tmp_path='/tmp/test', options=ConversionOptions()); print('Job OK:', j.options)"</automated>
  </verify>
  <done>
    - Job dataclass importabile con options: ConversionOptions
    - conversion_worker usa asyncio.Semaphore(MAX_CONCURRENT_JOBS)
    - MAX_CONCURRENT_JOBS definito in config.py (default 2)
    - Worker usa create_task pattern (non blocking sul loop)
    - task_done() nel finally di process_job
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /convert aggiornato con form fields opzioni</name>
  <files>backend/main.py</files>
  <behavior>
    - POST /convert con solo file (nessun form field) → funziona con ConversionOptions default
    - POST /convert con ocr_mode="on" → Job creato con options.ocr_mode="on"
    - POST /convert con table_detection=false → options.table_detection=False
    - POST /convert con page_from=2&page_to=5 → options.page_from=2, options.page_to=5
    - POST /convert con ocr_languages="it,en" → options.ocr_languages=["it","en"]
    - POST /convert con file PDF → risponde 202 {"job_id": "..."}
    - Tutte le validazioni esistenti (size, extension) rimangono invariate
  </behavior>
  <action>
Aggiorna backend/main.py:

1. Aggiungi import: `from fastapi import Form` (aggiungere a riga import fastapi esistente), `from typing import Optional`
2. Aggiungi import: `from adapter import ConversionOptions` (aggiungi alla riga import adapter)

3. Aggiorna signature di `convert()`:
```python
@app.post("/convert")
async def convert(
    request: Request,
    file: UploadFile,
    ocr_mode: str = Form(default="auto"),
    table_detection: bool = Form(default=True),
    page_from: Optional[int] = Form(default=None),
    page_to: Optional[int] = Form(default=None),
    ocr_languages: Optional[str] = Form(default=None),  # comma-separated
) -> JSONResponse:
```

4. Prima di `job = Job(...)`, costruisci ConversionOptions:
```python
options = ConversionOptions(
    ocr_mode=ocr_mode,
    table_detection=table_detection,
    page_from=page_from,
    page_to=page_to,
    ocr_languages=ocr_languages.split(",") if ocr_languages else [],
)
```

5. Aggiorna creazione Job per includere options:
```python
job = Job(job_id=job_id, tmp_path=tmp_path, options=options)
```

Mantieni invariato tutto il resto: validazione extension, chunked read, size check, log, risposta 202.

NOTA: Il tmp_path usa ancora l'estensione `.pdf` hardcoded — va bene per Phase 4 (PDF only). In futuro se si estende a più formati si userà l'estensione originale del file.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/backend && python -c "import main; print('main.py imports OK')"</automated>
  </verify>
  <done>
    - main.py importabile senza errori
    - Endpoint /convert accetta ocr_mode, table_detection, page_from, page_to, ocr_languages come Form params opzionali
    - ConversionOptions costruito dai form fields e passato al Job
    - Validazioni esistenti (size, extension) invariate
  </done>
</task>

</tasks>

<verification>
```bash
# Verifica che il backend si avvii senza errori (richiede Docker)
cd /Users/ricky/MyProjects/docling-webapp
docker compose build backend && docker compose up -d backend
sleep 5
curl -s http://localhost:3000/api/health | python3 -m json.tool

# Verifica endpoint /convert con form fields (richiede PDF di test)
curl -s -X POST http://localhost:3000/api/convert \
  -F "file=@/tmp/test.pdf" \
  -F "ocr_mode=auto" \
  -F "table_detection=true" | python3 -m json.tool
```
</verification>

<success_criteria>
- backend/adapter.py: ConversionOptions importabile, DoclingAdapter.convert_file accetta opts param
- backend/job_store.py: Job.options field presente, worker usa asyncio.Semaphore(MAX_CONCURRENT_JOBS)
- backend/config.py: MAX_CONCURRENT_JOBS con default 2
- backend/main.py: endpoint /convert legge form fields e costruisce ConversionOptions
- Tutti i file importabili senza errori Python
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/04-options-panel-batch-conversion/04-01-SUMMARY.md`
</output>

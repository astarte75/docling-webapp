---
phase: 04-options-panel-batch-conversion
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - frontend/src/hooks/useBatchUpload.ts
  - frontend/src/components/BatchFileRow.tsx
  - frontend/src/components/BatchList.tsx
autonomous: true
requirements:
  - BTCH-01
  - BTCH-02
  - BTCH-04

must_haves:
  truths:
    - "useBatchUpload gestisce un array di BatchFile, uploadando ogni file e aprendo uno stream SSE per-job"
    - "Lo stream SSE per un file viene aperto solo quando il job è in stato 'converting' (non pending) — evita exhaust connessioni browser"
    - "Gli aggiornamenti di stato usano functional setState per evitare stale closure con concurrent SSE callbacks"
    - "BatchList mostra ogni file in una riga compatta con filename, status badge e pulsante Riprova sugli errori"
    - "Il bottone 'Scarica ZIP' appare appena ≥1 file è done, mostra il conteggio e scarica via JSZip"
  artifacts:
    - path: "frontend/src/hooks/useBatchUpload.ts"
      provides: "Hook che gestisce upload + SSE per ogni file in batch, aggiorna status"
      exports: ["useBatchUpload"]
    - path: "frontend/src/components/BatchFileRow.tsx"
      provides: "Singola riga batch: filename, status badge, retry button su error"
      exports: ["BatchFileRow"]
    - path: "frontend/src/components/BatchList.tsx"
      provides: "Lista batch con rows + bottone ZIP download"
      exports: ["BatchList"]
  key_links:
    - from: "frontend/src/hooks/useBatchUpload.ts"
      to: "frontend/src/hooks/useJobStream.ts"
      via: "Riusa useJobStream per-job, apertura lazy quando status='converting'"
      pattern: "useJobStream"
    - from: "frontend/src/components/BatchList.tsx"
      to: "JSZip"
      via: "downloadBatchZip() — raccoglie markdown dei file 'done', genera blob, download"
      pattern: "JSZip"
    - from: "frontend/src/hooks/useBatchUpload.ts"
      to: "backend POST /convert"
      via: "fetch con FormData inclusi i campi options snapshotati"
      pattern: "FormData.*append.*ocr_mode"
---

<objective>
Crea il hook useBatchUpload e i componenti BatchFileRow + BatchList per la gestione e visualizzazione del batch.

Purpose: Soddisfa BTCH-01 (multi-file), BTCH-02 (status per-file), BTCH-04 (ZIP download). Il batch è l'estensione naturale del flusso single-file: ogni file ha il suo job_id, il suo stream SSE, il suo status.

Output: useBatchUpload hook, BatchFileRow component, BatchList component con ZIP download via JSZip.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/04-options-panel-batch-conversion/04-CONTEXT.md
@.planning/phases/04-options-panel-batch-conversion/04-RESEARCH.md

@.planning/phases/04-options-panel-batch-conversion/04-02-SUMMARY.md

<interfaces>
<!-- Contratti esistenti riusabili -->

From frontend/src/hooks/useJobStream.ts (current — non modificare):
```typescript
// Hook esistente — accetta jobId (string | null) e callbacks
useJobStream(jobId: string | null, callbacks: {
  onCompleted: (markdown: string) => void;
  onFailed: (message: string) => void;
})
// Apre SSE solo quando jobId !== null; si disconnette quando jobId torna null
```

From frontend/src/hooks/useUpload.ts (current):
```typescript
// TanStack Mutation — upload singolo file
const upload = useUpload();
const { job_id } = await upload.mutateAsync(file);
// NON riutilizzare per batch — usaBatchUpload gestisce l'upload direttamente con fetch
```

From frontend/src/types/job.ts (after 04-02):
```typescript
export interface ConversionOptions {
  ocrMode: OcrMode;
  tableDetection: boolean;
  pageFrom: number | null;
  pageTo: number | null;
  ocrLanguages: string[];
}

export interface BatchFile {
  id: string;
  file: File;
  options: ConversionOptions;
  status: BatchFileStatus;  // 'pending' | 'converting' | 'done' | 'error'
  jobId?: string;
  markdown?: string;
  errorMessage?: string;
}
```

From backend/main.py (after 04-01):
```
POST /api/convert (multipart/form-data):
  file: File
  ocr_mode: string (default "auto")
  table_detection: boolean (default true)
  page_from?: number
  page_to?: number
  ocr_languages?: string (comma-separated)
→ 202: { job_id: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useBatchUpload — upload multi-file con SSE per-job</name>
  <files>frontend/src/hooks/useBatchUpload.ts</files>
  <action>
Crea frontend/src/hooks/useBatchUpload.ts.

Il hook gestisce un array di BatchFile e le transizioni di stato per ogni file.

```typescript
import { useState, useCallback, useRef } from 'react';
import type { BatchFile, ConversionOptions } from '@/types/job';

// Internal helper: upload one file with its options snapshot
async function uploadFileWithOptions(
  file: File,
  opts: ConversionOptions
): Promise<string> {
  const formData = new FormData();
  formData.append('file', file);
  formData.append('ocr_mode', opts.ocrMode);
  formData.append('table_detection', String(opts.tableDetection));
  if (opts.pageFrom !== null) formData.append('page_from', String(opts.pageFrom));
  if (opts.pageTo !== null) formData.append('page_to', String(opts.pageTo));
  if (opts.ocrLanguages.length > 0) formData.append('ocr_languages', opts.ocrLanguages.join(','));

  const res = await fetch('/api/convert', { method: 'POST', body: formData });
  if (!res.ok) {
    const body = await res.json().catch(() => ({ error: 'Upload failed' }));
    throw new Error(body.error || 'Upload failed');
  }
  const data = await res.json();
  return data.job_id as string;
}

// Internal helper: connect to SSE stream and update item status
function connectJobStream(
  jobId: string,
  itemId: string,
  updateItem: (id: string, patch: Partial<BatchFile>) => void
): EventSource {
  const es = new EventSource(`/api/jobs/${jobId}/stream`);

  es.addEventListener('started', () => {
    updateItem(itemId, { status: 'converting' });
  });

  es.addEventListener('completed', (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    updateItem(itemId, { status: 'done', markdown: data.markdown });
    es.close();
  });

  es.addEventListener('failed', (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    updateItem(itemId, { status: 'error', errorMessage: data.message });
    es.close();
  });

  es.onerror = () => {
    updateItem(itemId, { status: 'error', errorMessage: 'Connection error' });
    es.close();
  };

  return es;
}

export function useBatchUpload() {
  const [files, setFiles] = useState<BatchFile[]>([]);
  // Keep EventSource refs to avoid opening duplicates
  const streamsRef = useRef<Map<string, EventSource>>(new Map());

  // Functional updater — safe for concurrent SSE callbacks (avoids stale closure)
  const updateItem = useCallback((id: string, patch: Partial<BatchFile>) => {
    setFiles(prev => prev.map(f => f.id === id ? { ...f, ...patch } : f));
  }, []);

  // Add files to batch with options snapshot, then upload each
  const addFiles = useCallback(async (newFiles: File[], opts: ConversionOptions) => {
    const items: BatchFile[] = newFiles.map(file => ({
      id: crypto.randomUUID(),
      file,
      options: { ...opts },  // snapshot
      status: 'pending' as const,
    }));

    setFiles(prev => [...prev, ...items]);

    // Upload each file and open SSE stream — sequential uploads (one per file per request)
    // SSE connection opened after upload returns job_id
    for (const item of items) {
      try {
        const jobId = await uploadFileWithOptions(item.file, item.options);
        updateItem(item.id, { jobId, status: 'converting' });

        // Open SSE stream only after upload succeeds (lazy — not on 'pending')
        const es = connectJobStream(jobId, item.id, updateItem);
        streamsRef.current.set(item.id, es);
      } catch (err) {
        const msg = err instanceof Error ? err.message : 'Upload failed';
        updateItem(item.id, { status: 'error', errorMessage: msg });
      }
    }
  }, [updateItem]);

  // Retry a failed file with its original options
  const retryFile = useCallback(async (itemId: string) => {
    const item = files.find(f => f.id === itemId);
    if (!item) return;

    // Close existing stream if any
    streamsRef.current.get(itemId)?.close();
    streamsRef.current.delete(itemId);

    updateItem(itemId, { status: 'pending', jobId: undefined, errorMessage: undefined });

    try {
      const jobId = await uploadFileWithOptions(item.file, item.options);
      updateItem(itemId, { jobId, status: 'converting' });
      const es = connectJobStream(jobId, itemId, updateItem);
      streamsRef.current.set(itemId, es);
    } catch (err) {
      const msg = err instanceof Error ? err.message : 'Upload failed';
      updateItem(itemId, { status: 'error', errorMessage: msg });
    }
  }, [files, updateItem]);

  const clearFiles = useCallback(() => {
    // Close all open streams
    streamsRef.current.forEach(es => es.close());
    streamsRef.current.clear();
    setFiles([]);
  }, []);

  return { files, addFiles, retryFile, clearFiles };
}
```

NOTA: Si usa direttamente `EventSource` invece di `useJobStream` perché useBatchUpload gestisce i propri stream internamente (fuori dal React lifecycle dei singoli componenti). `useJobStream` resta per il flusso single-file in App.tsx.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | grep -i "usebatchupload\|error" | head -20</automated>
  </verify>
  <done>
    - useBatchUpload.ts compila senza errori TypeScript
    - Esporta useBatchUpload hook
    - Gestisce upload sequenziale per ogni file
    - Apre SSE stream dopo upload (lazy, non su 'pending')
    - Usa functional setState per aggiornamenti batch-safe
    - Supporta retry per file in errore con le opzioni originali
    - clearFiles() chiude tutti gli stream aperti
  </done>
</task>

<task type="auto">
  <name>Task 2: BatchFileRow + BatchList con ZIP download</name>
  <files>frontend/src/components/BatchFileRow.tsx, frontend/src/components/BatchList.tsx</files>
  <action>
**frontend/src/components/BatchFileRow.tsx**

Componente che mostra una singola riga del batch:

```typescript
import type { BatchFile } from '@/types/job';
import { Button } from '@/components/ui/button';
import { Loader2 } from 'lucide-react';
import { cn } from '@/lib/utils';

// Status badge colors (discretionary)
const STATUS_BADGE: Record<string, { label: string; className: string }> = {
  pending:    { label: 'In attesa',    className: 'bg-muted text-muted-foreground' },
  converting: { label: 'Conversione', className: 'bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300' },
  done:       { label: 'Completato',  className: 'bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300' },
  error:      { label: 'Errore',      className: 'bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-300' },
};

interface BatchFileRowProps {
  item: BatchFile;
  onRetry: (id: string) => void;
}

export function BatchFileRow({ item, onRetry }: BatchFileRowProps) {
  const badge = STATUS_BADGE[item.status];

  return (
    <div className="flex items-center gap-3 py-2 px-3 rounded-md hover:bg-muted/30">
      {/* Filename */}
      <span className="flex-1 truncate text-sm font-medium" title={item.file.name}>
        {item.file.name}
      </span>

      {/* Status badge with spinner for converting */}
      <div className={cn('flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-medium', badge.className)}>
        {item.status === 'converting' && (
          <Loader2 className="h-3 w-3 animate-spin" />
        )}
        {badge.label}
      </div>

      {/* Error message inline (sotto il badge, visibile solo su error) */}
      {item.status === 'error' && item.errorMessage && (
        <span className="text-xs text-destructive truncate max-w-[200px]" title={item.errorMessage}>
          {item.errorMessage}
        </span>
      )}

      {/* Retry button — only on error */}
      {item.status === 'error' && (
        <Button variant="outline" size="sm" onClick={() => onRetry(item.id)}>
          Riprova
        </Button>
      )}
    </div>
  );
}
```

**frontend/src/components/BatchList.tsx**

Componente che mostra la lista completa e il bottone ZIP:

```typescript
import JSZip from 'jszip';
import type { BatchFile } from '@/types/job';
import { BatchFileRow } from '@/components/BatchFileRow';
import { Button } from '@/components/ui/button';

interface BatchListProps {
  files: BatchFile[];
  onRetry: (id: string) => void;
}

async function downloadBatchZip(files: BatchFile[]): Promise<void> {
  const zip = new JSZip();

  for (const f of files) {
    if (f.status === 'done' && f.markdown) {
      const basename = f.file.name.replace(/\.[^.]+$/, '');
      zip.file(`${basename}.md`, f.markdown);
    }
  }

  const blob = await zip.generateAsync({ type: 'blob' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'converted.zip';
  a.click();
  URL.revokeObjectURL(url);
}

export function BatchList({ files, onRetry }: BatchListProps) {
  const doneCount = files.filter(f => f.status === 'done').length;
  const hasActive = files.some(f => f.status === 'pending' || f.status === 'converting');

  return (
    <div className="rounded-lg border">
      {/* File list */}
      <div className="divide-y">
        {files.map(item => (
          <BatchFileRow key={item.id} item={item} onRetry={onRetry} />
        ))}
      </div>

      {/* ZIP download footer — shown when ≥1 file is done */}
      {doneCount > 0 && (
        <div className="border-t px-3 py-3 flex items-center justify-between">
          <span className="text-sm text-muted-foreground">
            {doneCount} file convertit{doneCount === 1 ? 'o' : 'i'}
            {hasActive && ' (conversioni in corso...)'}
          </span>
          <Button
            onClick={() => downloadBatchZip(files)}
            disabled={doneCount === 0}
          >
            Scarica ZIP ({doneCount} {doneCount === 1 ? 'file' : 'file'})
          </Button>
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | grep -i "batchlist\|batchfilerow\|error" | head -20</automated>
  </verify>
  <done>
    - BatchFileRow.tsx compila, mostra filename + status badge + spinner per 'converting' + retry button su 'error'
    - BatchList.tsx compila, mostra le righe e il footer ZIP quando doneCount > 0
    - ZIP download via JSZip — genera blob client-side con tutti i file 'done'
    - Bottone ZIP mostra conteggio file riusciti
    - Nessun errore TypeScript nel progetto
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ricky/MyProjects/docling-webapp/frontend
npx tsc --noEmit
npm run build 2>&1 | tail -30
```
</verification>

<success_criteria>
- useBatchUpload.ts: hook che gestisce array di BatchFile, upload con options, SSE lazy, retry
- BatchFileRow.tsx: riga compatta con status badge, spinner converting, retry button su error
- BatchList.tsx: lista + footer ZIP (doneCount >= 1), ZIP client-side via JSZip
- TypeScript compila senza errori
- Vite build passa
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/04-options-panel-batch-conversion/04-04-SUMMARY.md`
</output>

---
phase: 11-dockerhub-multi-arch-publish
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DCKR-04

must_haves:
  truths:
    - "README has a 'Quick Start (pre-built images)' section above the build-from-source section"
    - "README shows the exact command: docker compose -f docker-compose.prod.yml up -d"
    - "README explains the ~4 GB first-pull download size (Docling models pre-baked)"
    - "README explains the PORT env variable for the pre-built deploy method"
  artifacts:
    - path: "README.md"
      provides: "DockerHub deployment instructions"
      contains: "docker-compose.prod.yml"
  key_links:
    - from: "README.md Quick Start (pre-built images)"
      to: "docker-compose.prod.yml"
      via: "docker compose -f docker-compose.prod.yml up -d command"
      pattern: "docker-compose.prod.yml"
---

<objective>
Update README.md to add a "Quick Start (pre-built images)" section that shows users how to deploy without building locally.

Purpose: Users landing on the DockerHub page or GitHub repo can immediately find the no-build deploy path. This is the primary deployment method for v1.3.

Output: Updated README.md with pre-built images section.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dockerhub-multi-arch-publish/11-01-SUMMARY.md

<interfaces>
<!-- Current README.md structure (read the file before editing) -->

Current sections in order:
1. Title + description paragraph
2. ## Stack (table)
3. ### OCR Engines (table)
4. ### Supported Input Formats
5. ---
6. ## Quick Start (Docker Compose)
   - ### Requirements
   - ### Production (recommended) — shows: docker compose up --build -d
   - ### Development (hot reload)
7. ---
8. ## Local Development (no Docker)
9. ---
10. ## Platform Notes
11. ---
12. ## Configuration (env vars table)
13. ---
14. ## Project Structure

<!-- docker-compose.prod.yml created in Plan 01 -->
deploy command: docker compose -f docker-compose.prod.yml up -d
images: ricky75/docling-webapp-backend:latest, ricky75/docling-webapp-frontend:latest
pull_policy: always (no docker compose pull needed separately)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-built images section to README.md</name>
  <files>README.md</files>
  <action>
Read /Users/ricky/MyProjects/docling-webapp/README.md first, then edit it.

INSERT a new section immediately BEFORE the existing `## Quick Start (Docker Compose)` section.

The new section must be named `## Quick Start (pre-built images)` and contain:

1. A one-line intro: "Pull pre-built multi-arch images from DockerHub — no source code or build tools required."

2. Subsection `### Requirements`:
   - Docker Engine 24+ with Compose v2
   - ~4 GB disk space on first pull (Docling ML models are pre-baked in the image)
   - No GPU required (CPU inference)
   - Supported architectures: linux/amd64, linux/arm64

3. Subsection `### Deploy`:
   ```bash
   docker compose -f docker-compose.prod.yml up -d
   ```
   Followed by: "The app is available at **http://localhost:3000**."

4. Custom port example:
   ```bash
   PORT=8080 docker compose -f docker-compose.prod.yml up -d
   ```

5. Note about first-pull time: "> **Note:** The backend image is ~1.5 GB (Docling ML models pre-baked). First pull takes a few minutes depending on network speed. Subsequent starts are instant."

6. Update instructions (for existing users):
   ```bash
   docker compose -f docker-compose.prod.yml pull
   docker compose -f docker-compose.prod.yml up -d
   ```

7. DockerHub links as a brief note: "Images: [ricky75/docling-webapp-backend](https://hub.docker.com/r/ricky75/docling-webapp-backend) · [ricky75/docling-webapp-frontend](https://hub.docker.com/r/ricky75/docling-webapp-frontend)"

Add a `---` separator after the new section (before the existing `## Quick Start (Docker Compose)` section).

ALSO update the existing `## Quick Start (Docker Compose)` section heading to `## Quick Start (build from source)` to clearly distinguish the two methods.

Do NOT modify any other part of README.md. Preserve all existing content exactly.
  </action>
  <verify>
    <automated>grep -c "docker-compose.prod.yml" /Users/ricky/MyProjects/docling-webapp/README.md</automated>
  </verify>
  <done>
    - README contains `## Quick Start (pre-built images)` section
    - README contains `docker compose -f docker-compose.prod.yml up -d` command
    - README contains the ~1.5 GB first-pull note
    - The existing build-from-source section is renamed to `## Quick Start (build from source)`
    - `grep -c "docker-compose.prod.yml" README.md` returns at least 3 (deploy cmd, pull cmd, and link/reference)
  </done>
</task>

</tasks>

<verification>
After task completes:

1. Section exists: `grep "Quick Start (pre-built images)" README.md` — must match.

2. Command present: `grep "docker-compose.prod.yml" README.md` — must show at least 2 occurrences.

3. Build section renamed: `grep "Quick Start (build from source)" README.md` — must match.

4. Render check: Open README.md and verify the section ordering makes sense (pre-built first, then build-from-source).
</verification>

<success_criteria>
- README.md has a new `## Quick Start (pre-built images)` section as the primary deployment path
- Section includes the exact `docker compose -f docker-compose.prod.yml up -d` command
- Section includes first-pull size warning (~1.5 GB) and update instructions
- Existing build-from-source section is clearly labeled as secondary method
- All other README content is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-dockerhub-multi-arch-publish/11-02-SUMMARY.md` following the summary template.
</output>

---
phase: 08-ocr-engine-research-and-selection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "08-BENCHMARK.md exists in the phase directory"
    - "Document covers all 4 engines: Auto, EasyOCR, RapidOCR, Tesseract"
    - "Document includes accuracy, speed, Docker image size impact, language coverage per engine"
    - "Document gives a clear recommendation per document type (scanned PDF, native PDF with images, multilingual)"
    - "Document includes a summary table for quick reference"
  artifacts:
    - path: ".planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md"
      provides: "Written OCR engine comparison and recommendation guide"
      contains: "benchmark"
  key_links: []
---

<objective>
Produce 08-BENCHMARK.md: a written comparison of the 4 supported OCR engines (Auto, EasyOCR, RapidOCR, Tesseract) covering accuracy, speed, Docker size impact, language coverage, and a per-use-case recommendation. This is a manual comparison based on research data, not an automated benchmark script.

Purpose: Gives users a reference for choosing the right engine for their document type; documents trade-offs for future maintenance.
Output: .planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ocr-engine-research-and-selection/08-CONTEXT.md
@.planning/phases/08-ocr-engine-research-and-selection/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 08-BENCHMARK.md</name>
  <files>.planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md</files>
  <action>
Create 08-BENCHMARK.md using all data from 08-RESEARCH.md. The document must be written in English (technical documentation convention).

Structure:

# OCR Engine Benchmark — Docling Webapp

## Overview
Brief intro: what engines are compared, how data was gathered (research-based, not automated runs), confidence level.

## Engines Compared
Short paragraph per engine: what it is, how it works, installation requirements.
- Auto (OcrAutoOptions) — selects best available at runtime
- EasyOCR — deep learning, multilingual, already bundled
- RapidOCR (rapidocr-onnxruntime) — ONNX-based, lightweight, fast on CPU
- Tesseract (tesserocr binding) — traditional OCR, requires system packages

## Comparison Table
| Dimension | Auto | EasyOCR | RapidOCR | Tesseract |
Use RESEARCH.md table as base. Dimensions: Accuracy (printed text), Accuracy (handwritten), CPU speed (A4 page estimate), GPU acceleration, Docker image size impact, Multilingual support, Python install, System dependencies.

## Recommendations by Document Type

### Scanned PDF (no native text layer)
Recommend engine + rationale.

### Native PDF with embedded images
Recommend engine + rationale.

### Multilingual document
Recommend engine + rationale.

### Fast CPU-only processing
Recommend engine + rationale.

### General use / unknown document
Recommend engine + rationale (likely Auto).

## Known Limitations

- Tesseract multilingual: only English pack installed by default; other languages require Dockerfile customization (`apt-get install tesseract-ocr-XXX`)
- RapidOCR language model: EN/ZH by default; additional models need Docling model download
- ocr_languages field (language selector in UI): applies ONLY to EasyOCR — ignored for Tesseract and RapidOCR in this implementation
- Auto engine: picks "best available" — may not pick the engine the user expects; does NOT try all engines
- Accuracy data confidence: MEDIUM — based on community benchmarks, not project-specific runs

## Data Sources
List primary and secondary sources from RESEARCH.md Sources section.

---
*Written: 2026-03-03*
*Based on Docling research — valid until ~2026-04-03 (verify if docling version upgraded)*
  </action>
  <verify>
    <automated>test -f /Users/ricky/MyProjects/docling-webapp/.planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md && echo "BENCHMARK.md exists" && wc -l /Users/ricky/MyProjects/docling-webapp/.planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md</automated>
  </verify>
  <done>08-BENCHMARK.md exists, covers all 4 engines, includes comparison table, per-use-case recommendations, and known limitations section; at least 80 lines</done>
</task>

</tasks>

<verification>
```bash
test -f /Users/ricky/MyProjects/docling-webapp/.planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md \
  && grep -c "##" /Users/ricky/MyProjects/docling-webapp/.planning/phases/08-ocr-engine-research-and-selection/08-BENCHMARK.md
```
Must find file with at least 4 section headers.
</verification>

<success_criteria>
- 08-BENCHMARK.md exists in the phase directory
- All 4 engines covered with install requirements
- Comparison table present with accuracy/speed/size/language dimensions
- Per-use-case recommendations for at least 3 document types
- Known limitations section documents the ocr_languages EasyOCR-only behaviour and Tesseract language pack requirement
</success_criteria>

<output>
After completion, create `.planning/phases/08-ocr-engine-research-and-selection/08-03-SUMMARY.md`
</output>

---
phase: 07-visual-redesign-frontend-design
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/UploadZone.tsx
  - frontend/src/components/ResultViewer.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Il toggle Sun/Moon appare nell'header a destra del titolo"
    - "Cliccando il toggle l'interfaccia cambia tra light e dark"
    - "La scelta del tema persiste dopo refresh della pagina (localStorage)"
    - "L'UploadZone ha bordo emerald solido (non tratteggiato) in idle"
    - "Durante drag-over l'UploadZone mostra glow verde e sfondo tenue emerald"
    - "Il ResultViewer mostra la gerarchia titoli h1>h2>h3 con size distinte"
    - "Il ResultViewer si adatta correttamente al dark mode (testo leggibile su sfondo scuro)"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "App wrappata in ThemeProvider con ModeToggle nell'header"
      contains: "ThemeProvider"
    - path: "frontend/src/components/UploadZone.tsx"
      provides: "Bordo emerald solido + glow drag-over"
      contains: "emerald-500"
    - path: "frontend/src/components/ResultViewer.tsx"
      provides: "Tipografia curata con gerarchia titoli e dark:prose-invert"
      contains: "dark:prose-invert"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/theme-provider.tsx"
      via: "ThemeProvider wrapping del JSX"
      pattern: "<ThemeProvider"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/mode-toggle.tsx"
      via: "ModeToggle nell'header"
      pattern: "<ModeToggle"
    - from: "frontend/src/components/ResultViewer.tsx"
      to: "@tailwindcss/typography"
      via: "prose-h1: prose-h2: prose-h3: element modifiers + dark:prose-invert"
      pattern: "prose-invert"
---

<objective>
Integrare ThemeProvider e ModeToggle in App.tsx, aggiornare UploadZone con il look emerald e glow drag-over, migliorare la tipografia del ResultViewer con gerarchia titoli e dark mode support.

Purpose: Wiring finale del sistema di theming e applicazione delle classi visive ai componenti chiave. Dopo questo plan il redesign è completo e pronto per la verifica umana.
Output: App.tsx con ThemeProvider + toggle, UploadZone con bordo e glow emerald, ResultViewer con tipografia curata e dark mode.
</objective>

<execution_context>
@/Users/ricky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-visual-redesign-frontend-design/07-CONTEXT.md
@.planning/phases/07-visual-redesign-frontend-design/07-RESEARCH.md
@.planning/phases/07-visual-redesign-frontend-design/07-01-SUMMARY.md

<interfaces>
<!-- Struttura attuale App.tsx (semplificata) — preservare tutta la logica esistente -->
From frontend/src/App.tsx:
```typescript
// Imports esistenti — aggiungere:
import { ThemeProvider } from '@/components/theme-provider'
import { ModeToggle } from '@/components/mode-toggle'

// Header attuale (da modificare):
<header className="mb-8 text-center">
  <h1 className="text-2xl font-semibold tracking-tight cursor-pointer select-none hover:opacity-70 transition-opacity"
      onClick={...} role="button" tabIndex={0} onKeyDown={...}>
    Docling Webapp
  </h1>
  <p className="mt-1 text-sm text-muted-foreground">
    Convert documents to Markdown
  </p>
</header>

// Header target — flex con spacer per centrare il titolo e toggle a destra:
<header className="mb-8">
  <div className="flex items-center justify-between">
    <div className="flex-1" />
    <h1 className="text-2xl font-semibold tracking-tight cursor-pointer select-none hover:opacity-70 transition-opacity text-center"
        onClick={...} role="button" tabIndex={0} onKeyDown={...}>
      Docling Webapp
    </h1>
    <div className="flex-1 flex justify-end">
      <ModeToggle />
    </div>
  </div>
  <p className="mt-2 text-sm text-muted-foreground text-center">
    Convert documents to Markdown
  </p>
</header>

// Wrapping: ThemeProvider avvolge il JSX completo
return (
  <ThemeProvider defaultTheme="system" storageKey="docling-theme">
    <div className="min-h-screen bg-background text-foreground">
      ...
    </div>
  </ThemeProvider>
)
```

<!-- Struttura attuale UploadZone.tsx hero mode (da modificare) -->
From frontend/src/components/UploadZone.tsx:
```typescript
// Classe attuale — hero mode (non compact):
className={[
  'w-full rounded-xl border-2 border-dashed px-8 py-12 text-center',
  'transition-colors cursor-pointer select-none',
  isDragActive
    ? 'border-primary bg-primary/5 text-primary'
    : 'border-muted-foreground/30 hover:border-primary/60 hover:bg-muted/40',
  disabled ? 'pointer-events-none opacity-50' : '',
].join(' ')}

// Target — bordo solido emerald + glow drag-over:
className={[
  'w-full rounded-xl border-2 px-8 py-12 text-center',
  'transition-all duration-200 cursor-pointer select-none',
  isDragActive
    ? [
        'border-emerald-500 border-solid',
        'bg-emerald-50/60 dark:bg-emerald-950/30',
        'shadow-lg shadow-emerald-500/40',
        'text-emerald-700 dark:text-emerald-300',
      ].join(' ')
    : [
        'border-emerald-600/40',
        'hover:border-emerald-500 hover:bg-emerald-50/30',
        'dark:border-emerald-700/40 dark:hover:border-emerald-500',
      ].join(' '),
  disabled ? 'pointer-events-none opacity-50' : '',
].join(' ')}
// NOTA: border-dashed RIMOSSO — non usarlo più. Border-solid è il default CSS.
// UploadCloud icon: aggiungere text-emerald-600 dark:text-emerald-400 per coerenza cromatica
```

<!-- Struttura attuale ResultViewer.tsx prose (da modificare) -->
From frontend/src/components/ResultViewer.tsx:
```typescript
// Classe attuale:
<div className="prose prose-sm max-w-none">

// Target — tipografia curata + dark mode + gerarchia titoli:
<div className={[
  "prose prose-sm max-w-none",
  "dark:prose-invert",
  "prose-h1:text-2xl prose-h1:font-bold prose-h1:leading-tight",
  "prose-h2:text-xl prose-h2:font-semibold prose-h2:leading-snug",
  "prose-h3:text-lg prose-h3:font-medium prose-h3:leading-snug",
  "prose-p:leading-relaxed",
].join(" ")}>

// TabsContent padding: aumentare da p-4 a p-6 per il breathing room richiesto
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapping App.tsx con ThemeProvider e ModeToggle nell'header</name>
  <files>frontend/src/App.tsx</files>
  <action>
Modificare App.tsx con tre cambiamenti:

1. **Aggiungere import** in cima al file (dopo gli import esistenti):
   ```typescript
   import { ThemeProvider } from '@/components/theme-provider'
   import { ModeToggle } from '@/components/mode-toggle'
   ```

2. **Aggiornare l'header** da `text-center` semplice a layout flex con spacer:
   Il titolo deve rimanere visivamente centrato. Il toggle va a destra. Struttura:
   ```jsx
   <header className="mb-8">
     <div className="flex items-center justify-between">
       <div className="flex-1" />
       <h1
         className="text-2xl font-semibold tracking-tight cursor-pointer select-none hover:opacity-70 transition-opacity"
         onClick={...}  {/* preservare handler esistente */}
         role="button"
         tabIndex={0}
         onKeyDown={...}  {/* preservare handler esistente */}
       >
         Docling Webapp
       </h1>
       <div className="flex-1 flex justify-end">
         <ModeToggle />
       </div>
     </div>
     <p className="mt-2 text-sm text-muted-foreground text-center">
       Convert documents to Markdown
     </p>
   </header>
   ```

3. **Wrappare il return** in ThemeProvider:
   ```jsx
   return (
     <ThemeProvider defaultTheme="system" storageKey="docling-theme">
       <div className="min-h-screen bg-background text-foreground">
         ...  {/* contenuto esistente invariato */}
       </div>
     </ThemeProvider>
   )
   ```

PRESERVARE tutta la logica esistente: handleFilesSelected, useJobStream, useBatchUpload, state machine, tutti i componenti renderizzati. Solo l'header e il wrapping cambiano.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compila senza errori. App.tsx importa ThemeProvider e ModeToggle, li usa nel JSX. Logica esistente preservata intatta.</done>
</task>

<task type="auto">
  <name>Task 2: Stile emerald UploadZone e tipografia ResultViewer</name>
  <files>frontend/src/components/UploadZone.tsx, frontend/src/components/ResultViewer.tsx</files>
  <action>
**UploadZone.tsx — hero mode (non compact):**

Sostituire le classi del div `getRootProps()` in modalità hero (il secondo blocco di return, non il `compact` branch):
- Rimuovere `border-dashed` — il bordo diventa solido (default CSS)
- Cambiare `transition-colors` in `transition-all duration-200`
- Sostituire le classi isDragActive e idle con le classi emerald definite nell'interfaccia context
- Aggiungere `text-emerald-600 dark:text-emerald-400` alla UploadCloud icon per coerenza cromatica

La modalità `compact` (batch strip) NON deve essere modificata — lasciare le classi existenti.

**ResultViewer.tsx:**

1. Aggiornare il div.prose: da `"prose prose-sm max-w-none"` al className multi-riga con:
   - `dark:prose-invert`
   - `prose-h1:text-2xl prose-h1:font-bold prose-h1:leading-tight`
   - `prose-h2:text-xl prose-h2:font-semibold prose-h2:leading-snug`
   - `prose-h3:text-lg prose-h3:font-medium prose-h3:leading-snug`
   - `prose-p:leading-relaxed`

2. Aggiornare i TabsContent da `p-4` a `p-6` per entrambi (preview e raw) — breathing room aggiuntivo per lettura comoda.

Tutta la logica di download, copy, useState, tabs rimane invariata.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/docling-webapp/frontend && npx tsc --noEmit 2>&1 | head -20 && grep -c "emerald" src/components/UploadZone.tsx && grep -c "prose-invert" src/components/ResultViewer.tsx</automated>
  </verify>
  <done>TypeScript compila senza errori. UploadZone.tsx contiene almeno 1 occorrenza di "emerald". ResultViewer.tsx contiene "prose-invert". Nessun "border-dashed" residuale nell'UploadZone hero mode.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes con 0 errori
- `grep "ThemeProvider" frontend/src/App.tsx` trova sia import che uso JSX
- `grep "ModeToggle" frontend/src/App.tsx` trova sia import che uso JSX
- `grep "border-dashed" frontend/src/components/UploadZone.tsx` non trova occorrenze nel hero mode
- `grep "emerald" frontend/src/components/UploadZone.tsx` trova classi emerald
- `grep "prose-invert" frontend/src/components/ResultViewer.tsx` trova la classe
- `grep "prose-h1" frontend/src/components/ResultViewer.tsx` trova i modificatori heading
</verification>

<success_criteria>
Il redesign visivo è completamente integrato: tema emerald attivo, toggle dark/light nell'header funzionante, UploadZone con bordo solido verde e glow drag-over, ResultViewer con tipografia curata e dark mode. L'app compila senza errori TypeScript.
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-frontend-design/07-02-SUMMARY.md`
</output>
